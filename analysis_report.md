## 智能羽毛球陪练系统 — 数据接入分析报告

### 系统现状概览

| 维度 | 统计 |
|------|------|
| 后端API总端点数 | **36个** |
| 前端实际调用的端点数 | **16个** (44%) |
| 已实现但未调用的端点数 | **19个** (53%) |
| 需要新增的端点数 | **约9个** |
| ESP8266当前发送字段 | **9个** (temp, humi, light, smoke, wind_speed, pressure, uwb_x/y/z) |
| ESP8266需要新增的字段 | **约17个** |
| 前端硬编码数据块 | **30+处** |
| 已发现的BUG | **4个** |

---

### 已发现的BUG (需立即修复)

1. **数据分析页图表数据错位**: 命中率趋势图显示了温度数据，速度分布图显示了湿度数据。原因是 `/api/sensors/history` 返回的传感器数据被错误映射到了训练图表。
2. **用户管理页字段名不匹配**: 前端读 `total_sessions`/`total_hours`，后端返回 `total_training_count`/`total_training_hours`，导致显示 `--`。
3. **训练历史表格字段名不匹配**: 前端读 `t.date`/`t.duration`/`t.servings`，后端返回 `created_at`/`duration_minutes`/`serving_count`。
4. **数据库字段映射需确认**: `mqtt.js` INSERT 用 `temperature`/`humidity`，前端读 `s.temperature`，应确保一致。

---

### 逐页分析

#### 1. 控制台 (Dashboard) — 完成度 ~60%

**已动态加载**: 在线设备数、今日训练时长、总发球数、命中率、告警数、环境数据(风速/温度/湿度/气压)

**仍为硬编码**:
- 训练趋势折线图: 数据写死 `[1.2,1.5,0.8,1.8,1.3,1.0,1.5]`，应从 `/api/training/stats` 获取
- 模式分布饼图: 数据写死 `[45,30,25]`，应从训练统计获取
- 电机状态卡片: RPM=2450, 发球速度=85km/h, 运行时长=01:23 — 需要ESP8266上报电机数据

#### 2. 训练中心 (Training) — 完成度 ~50%

**已动态加载**: 控制操作(开始/暂停/停止/切换模式/参数调整) 都正确调用后端API

**仍为硬编码**:
- 实时训练图表: 初始数据写死，无实时更新机制
- 训练状态指示: "训练中"绿色圆点始终显示，不反映实际状态
- 参数默认值: 速度85、间隔2.0s、时长45min — 应从 `/api/settings` 加载
- **缺失**: 切换页面时不调用 `GET /api/training/current` 恢复训练状态

#### 3. 球场视图 (Court) — 完成度 ~15%

**已动态加载**: UWB坐标、SVG设备标记位置、4条TOF测距连线

**仍为硬编码** (严重):
- UWB基站状态面板: 4个基站的距离/信号/延迟全部写死
- 右侧实时状态: 落点区域=A区, 发球速度=85km/h, 已发球=156, 命中率=78%
- 落点区域命中率: A区82%, B区75%... SVG文本写死
- 落点标记: 12个静态圆点，位置固定
- 球员位置标记: 固定在SVG(250,340)
- 球员运动数据: 移动距离1245m, 最高速度6.8m/s 等全部写死
- 最近发球序列表格: 6条记录完全写死
- 落点分布饼图: 数据写死

#### 4. 数据分析 (Analytics) — 完成度 ~40%

**已动态加载**: 训练统计KPI (总时长/总发球/命中率/最高球速)、训练历史表格、传感器历史图表

**BUG**: 命中率趋势图实际显示温度数据，速度分布图实际显示湿度数据

**字段不匹配**: 训练历史表格字段名与后端返回不一致

#### 5. 设备管理 (Devices) — 完成度 ~80%

**已动态加载**: 设备列表(名称/序列号/状态/固件/电量)、设备运行日志

**未使用**: `GET /api/devices/:id` (详情接口)、远程控制功能

#### 6. 告警中心 (Alerts) — 完成度 ~65%

**已动态加载**: 告警统计KPI、告警列表、解决告警

**未使用**: 确认告警、删除告警、告警阈值配置、告警过滤

#### 7. 用户管理 (Users) — 完成度 ~60%

**已动态加载**: 用户列表渲染

**BUG**: 字段名不匹配导致显示 `--`。**未使用**: 添加/编辑/删除用户API

#### 8. 系统设置 (Settings) — 完成度 ~80%

**已动态加载**: 设置加载、保存、同步到设备 — 均正常工作

**仍为硬编码**: 功能开关状态不从API加载、系统信息(版本/运行时间/存储)写死

---

### ESP8266 需要新增的上报数据

#### P0 — 核心 (优先级最高)

| 字段 | 用途 |
|------|------|
| `motor_rpm` | 控制台电机状态、设备管理 |
| `serving_speed` | 训练中心实时图表、球场视图 |
| `serving_count` | 球场已发球数、训练统计 |
| `accuracy` | 训练中心、球场命中率 |
| `training_mode` | 训练中心模式显示 |
| `training_status` | 训练中心状态指示(running/paused/stopped) |

#### P1 — 重要

| 字段 | 用途 |
|------|------|
| `battery_level` | 设备管理卡片 |
| `firmware_version` | 设备管理卡片 |
| `runtime_seconds` | 设备管理运行时长 |
| `wifi_rssi` | 设备管理、告警判断 |
| `error_count` | 控制台电机异常次数 |

#### P2 — 增强

| 字段 | 用途 |
|------|------|
| `ball_speed_history[]` | 训练中心实时图表 |
| `serving_sequence[]` | 球场发球序列 |
| `player_x`, `player_y` | 球场球员位置追踪 |
| `zone_hit_counts{A-F}` | 球场落点分布 |
| `movement_distance` | 球员运动数据 |
| `max_player_speed` | 球员运动数据 |

---

### 需要新增的后端API

| 端点 | 用途 | 优先级 |
|------|------|--------|
| `GET /api/training/trend` | 训练趋势(命中率/时长按日聚合) | P0 |
| `GET /api/training/live` | 当前训练实时数据流 | P0 |
| `GET /api/training/:id/servings` | 单次训练逐球记录 | P1 |
| `GET /api/training/speed-distribution` | 发球速度分布直方图 | P1 |
| `GET /api/training/accuracy-trend` | 命中率趋势(按周/月聚合) | P1 |
| `GET /api/court/realtime` | 球场实时综合数据 | P1 |
| `GET /api/court/heatmap` | 球场热力图数据 | P2 |
| `GET /api/system/status` | 系统运行状态 | P2 |
| `POST /api/devices/:id/control` | 设备远程控制 | P2 |

---

### 建议实施顺序

**第一阶段 (P0 — 核心修复)**:
1. 修复4个已知BUG (字段映射、图表数据错位)
2. 训练中心: 接入 `GET /api/training/current` 恢复状态
3. 控制台: 训练趋势图和模式分布图接入真实数据
4. ESP8266 扩展: 增加 motor_rpm, serving_speed, serving_count, accuracy, training_mode, training_status

**第二阶段 (P1 — 功能完善)**:
5. 球场视图: 动态落点、发球序列、球员运动数据
6. ESP8266 扩展: battery_level, firmware_version, wifi_rssi
7. 数据分析: 修复图表数据源，增加真正的命中率和速度趋势接口
8. 告警阈值管理接入前端

**第三阶段 (P2 — 增强功能)**:
9. 用户CRUD完整化
10. 数据导出功能
11. 球场热力图
12. 系统状态面板接入真实数据
13. LED/设备远程控制界面
